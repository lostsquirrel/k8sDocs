<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Kubernetes – 工作负载(Workload)</title>
    <link>https://lostsquirrel.github.io/k8sDocs/docs/concepts/workloads/</link>
    <description>Recent content in 工作负载(Workload) on Kubernetes</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>zh-cn</language>
    <lastBuildDate>Thu, 02 Jul 2020 00:00:00 +0000</lastBuildDate>
    
	  <atom:link href="https://lostsquirrel.github.io/k8sDocs/docs/concepts/workloads/index.xml" rel="self" type="application/rss+xml" />
    
    
      
        
      
    
    
    <item>
      <title>Docs: Pod</title>
      <link>https://lostsquirrel.github.io/k8sDocs/docs/concepts/workloads/pods/</link>
      <pubDate>Thu, 02 Jul 2020 00:00:00 +0000</pubDate>
      
      <guid>https://lostsquirrel.github.io/k8sDocs/docs/concepts/workloads/pods/</guid>
      <description>
        
        
        &lt;!-- overview --&gt;
&lt;!--
_Pods_ are the smallest deployable units of computing that you can create and manage in Kubernetes.

A _Pod_ (as in a pod of whales or pea pod) is a group of one or more
&lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/overview/what-is-kubernetes/#why-containers&#39; target=&#39;_blank&#39;&gt;containers&lt;span class=&#39;tooltip-text&#39;&gt;一个包含应用及其所有信赖的轻量的可移植的可运行的镜像&lt;/span&gt;
&lt;/a&gt;, with shared storage/network resources, and a specification
for how to run the containers. A Pod&#39;s contents are always co-located and
co-scheduled, and run in a shared context. A Pod models an
application-specific &#34;logical host&#34;: it contains one or more application
containers which are relatively tightly coupled.
In non-cloud contexts, applications executed on the same physical or virtual machine are analogous to cloud applications executed on the same logical host.

As well as application containers, a Pod can contain
[init containers](/docs/concepts/workloads/pods/init-containers/) that run
during Pod startup. You can also inject
[ephemeral containers](/docs/concepts/workloads/pods/ephemeral-containers/)
for debugging if your cluster offers this.
--&gt;
&lt;p&gt;&lt;em&gt;Pods&lt;/em&gt; 是在 k8s 集群中可创建和管理的最小可部署计算单元。&lt;/p&gt;
&lt;p&gt;一个 &lt;em&gt;Pods&lt;/em&gt; (可以类比一个豌豆荚或一个鲸落)就是由一组共享存储/网络，并带有运行指示的一个或多个&lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/overview/what-is-kubernetes/#why-containers&#39; target=&#39;_blank&#39;&gt;容器&lt;span class=&#39;tooltip-text&#39;&gt;一个包含应用及其所有信赖的轻量的可移植的可运行的镜像&lt;/span&gt;
&lt;/a&gt;。 同一个 Pod 中的&lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/overview/what-is-kubernetes/#why-containers&#39; target=&#39;_blank&#39;&gt;容器&lt;span class=&#39;tooltip-text&#39;&gt;一个包含应用及其所有信赖的轻量的可移植的可运行的镜像&lt;/span&gt;
&lt;/a&gt;共享同一个上下文并始终统一调度并最终落地在一个&lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/2-concepts/01-architecture/00-nodes/&#39; target=&#39;_blank&#39;&gt;节点&lt;span class=&#39;tooltip-text&#39;&gt;一个节点就是 k8s 中的一个工作机&lt;/span&gt;
&lt;/a&gt;. 一个 Pod 的模型为应用专用的逻辑主机： 包含一个或多个紧密关联的应用容器。 应用运行在非云环境情景的同一个物理或虚拟机上，与云应用运行在同一个逻辑主机上。&lt;/p&gt;
&lt;p&gt;Pod 在包含应用容器的同时也可以加入&lt;a href=&#34;../00-init-containers/&#34;&gt;初始化容器&lt;/a&gt;,用于在 Pod 启动时执行初始化工作，和 &lt;a href=&#34;../05-ephemeral-containers/&#34;&gt;临时容器&lt;/a&gt;用于调试。&lt;/p&gt;
&lt;!-- body --&gt;
&lt;h2 id=&#34;pod-是个啥东西&#34;&gt;Pod 是个啥东西?&lt;/h2&gt;
&lt;!--
&lt;blockquote class=&#34;note&#34;&gt;
  &lt;div&gt;&lt;strong&gt;说明：&lt;/strong&gt; While Kubernetes supports more
&lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/docs/setup/production-environment/container-runtimes&#39; target=&#39;_blank&#39;&gt;container runtimes&lt;span class=&#39;tooltip-text&#39;&gt;容器运行环境就是负责运行容器的软件&lt;/span&gt;
&lt;/a&gt;
than just Docker, &lt;a href=&#34;https://www.docker.com/&#34;&gt;Docker&lt;/a&gt; is the most commonly known
runtime, and it helps to describe Pods using some terminology from Docker.&lt;/div&gt;
&lt;/blockquote&gt;


The shared context of a Pod is a set of Linux namespaces, cgroups, and
potentially other facets of isolation - the same things that isolate a Docker
container.  Within a Pod&#39;s context, the individual applications may have
further sub-isolations applied.

In terms of Docker concepts, a Pod is similar to a group of Docker containers
with shared namespaces and shared filesystem volumes.
--&gt;
&lt;blockquote class=&#34;note&#34;&gt;
  &lt;div&gt;&lt;strong&gt;说明：&lt;/strong&gt; k8s 支持的 &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/docs/setup/production-environment/container-runtimes&#39; target=&#39;_blank&#39;&gt;容器运行环境&lt;span class=&#39;tooltip-text&#39;&gt;容器运行环境就是负责运行容器的软件&lt;/span&gt;
&lt;/a&gt;不止有 Docker, &lt;a href=&#34;https://www.docker.com/&#34;&gt;Docker&lt;/a&gt; 只是大家比较熟悉的容器运行环境，但是也可能通过 Docker 的一些术语来帮助解说 Pod&lt;/div&gt;
&lt;/blockquote&gt;

&lt;p&gt;Pod 中共享的上下文包括 Linux namespaces, cgroups, 和其它方面的隔离， 与 Docker 容器的隔离范围相同。 在一个 Pod 的上下文中，各个不同的应用可能还有更细分的隔离设置。&lt;/p&gt;
&lt;p&gt;以 Docker 的概念上来说， 一个 Pod 与 一组共享 namespaces 和文件系统卷的容器相似。&lt;/p&gt;
&lt;h2 id=&#34;pod-怎么用&#34;&gt;Pod 怎么用&lt;/h2&gt;
&lt;!--
Usually you don&#39;t need to create Pods directly, even singleton Pods. Instead, create them using workload resources such as &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/deployment/&#39; target=&#39;_blank&#39;&gt;Deployment&lt;span class=&#39;tooltip-text&#39;&gt;管理集群中的一个多副本应用&lt;/span&gt;
&lt;/a&gt; or &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/job/&#39; target=&#39;_blank&#39;&gt;Job&lt;span class=&#39;tooltip-text&#39;&gt;一个运行后即结束的有限或批量任务&lt;/span&gt;
&lt;/a&gt;.
If your Pods need to track state, consider the
&lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/statefulset/&#39; target=&#39;_blank&#39;&gt;StatefulSet&lt;span class=&#39;tooltip-text&#39;&gt;管理一个 Pod 集合的部署与容量伸缩， 这些 Pod 所以使用的存储是持久的(Pod 被替代后，新的 Pod 继承老 Pod 的存储)， Pod 的标识也是持久化的(重建 Pod 后名字不会变)&lt;/span&gt;
&lt;/a&gt; resource.
--&gt;
&lt;p&gt;通常用户不需要直接创建 Pod， 即使是单例 Pod(?啥玩意儿). 而是通过创建类似 &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/deployment/&#39; target=&#39;_blank&#39;&gt;Deployment&lt;span class=&#39;tooltip-text&#39;&gt;管理集群中的一个多副本应用&lt;/span&gt;
&lt;/a&gt; 或 &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/job/&#39; target=&#39;_blank&#39;&gt;Job&lt;span class=&#39;tooltip-text&#39;&gt;一个运行后即结束的有限或批量任务&lt;/span&gt;
&lt;/a&gt; 的工作负载资源间接创建 Pod。如果 Pod 需要跟踪状态(目前理解是包含持久化数据)，可以考虑使用 &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/statefulset/&#39; target=&#39;_blank&#39;&gt;StatefulSet&lt;span class=&#39;tooltip-text&#39;&gt;管理一个 Pod 集合的部署与容量伸缩， 这些 Pod 所以使用的存储是持久的(Pod 被替代后，新的 Pod 继承老 Pod 的存储)， Pod 的标识也是持久化的(重建 Pod 后名字不会变)&lt;/span&gt;
&lt;/a&gt; 资源。&lt;/p&gt;
&lt;!--
Pods in a Kubernetes cluster are used in two main ways:

* **Pods that run a single container**. The &#34;one-container-per-Pod&#34; model is the
  most common Kubernetes use case; in this case, you can think of a Pod as a
  wrapper around a single container; Kubernetes manages Pods rather than managing
  the containers directly.
* **Pods that run multiple containers that need to work together**. A Pod can
  encapsulate an application composed of multiple co-located containers that are
  tightly coupled and need to share resources. These co-located containers
  form a single cohesive unit of service—for example, one container serving data
  stored in a shared volume to the public, while a separate _sidecar_ container
  refreshes or updates those files.  
  The Pod wraps these containers, storage resources, and an ephemeral network
  identity together as a single unit.

  &lt;blockquote class=&#34;note&#34;&gt;
  &lt;div&gt;&lt;strong&gt;说明：&lt;/strong&gt; Grouping multiple co-located and co-managed containers in a single Pod is a
relatively advanced use case. You should use this pattern only in specific
instances in which your containers are tightly coupled.&lt;/div&gt;
&lt;/blockquote&gt;

--&gt;
&lt;p&gt;在 k8s 集群中 Pod 主要有以下两种使用方式:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;在 Pod 中运行一个容器&lt;/strong&gt;。 一个 Pod 装一个容器的的模式是 k8s 最常见的应用方式，就可以认为Pod就是给容器包了一个壳, 因为 k8s 管理的是 Pod 而不直接管理容器&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;在 Pod 中运行多个紧密相关的容器&lt;/strong&gt;。 一个 Pod 可以封装一个由多个紧密耦合在一起，需要共享资源的多个容器组成的应用。这些容器最终组合成一个服务， 例如一个容器对外提供存储在共享数据卷中的数据， 另一个 &lt;em&gt;边车&lt;/em&gt; 容器负责刷新或更新这些文件。
Pod 把这些容器，存储资源和一个临时的网络地址打包成一个整体。&lt;/p&gt;
&lt;blockquote class=&#34;note&#34;&gt;
  &lt;div&gt;&lt;strong&gt;说明：&lt;/strong&gt; 在一个Pod 中包含多个相互关联共同管理的容器是一种相对高级的应用场景。用户应该在应用中的容器之间紧密耦合的时候使用这种方式。&lt;/div&gt;
&lt;/blockquote&gt;

&lt;/li&gt;
&lt;/ul&gt;
&lt;!--
Each Pod is meant to run a single instance of a given application. If you want to
scale your application horizontally (to provide more overall resources by running
more instances), you should use multiple Pods, one for each instance. In
Kubernetes, this is typically referred to as _replication_.
Replicated Pods are usually created and managed as a group by a workload resource
and its &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/2-concepts/01-architecture/02-controller/&#39; target=&#39;_blank&#39;&gt;controller&lt;span class=&#39;tooltip-text&#39;&gt;一个控制回路，负责通过 apiserver 监控集群的共享状态，并尝试通过变更(某些对象)的方式实现集群从当前状态向期望状态迁移&lt;/span&gt;
&lt;/a&gt;.

See [Pods and controllers](#pods-and-controllers) for more information on how
Kubernetes uses workload resources, and their controllers, to implement application
scaling and auto-healing.
--&gt;
&lt;p&gt;每一个Pod 表示运行该应用的一个实例。 如果想要水平扩展应用(通过运行更多的实例来提供更多的资源)，则需要运行多个 Pod， 一个Pod对应一个实例。在 k8s 中，这通常被称为 &lt;em&gt;副本(replication)&lt;/em&gt;
这些副本 Pod 通常由一个工作负载资源也就是 &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/2-concepts/01-architecture/02-controller/&#39; target=&#39;_blank&#39;&gt;controller&lt;span class=&#39;tooltip-text&#39;&gt;一个控制回路，负责通过 apiserver 监控集群的共享状态，并尝试通过变更(某些对象)的方式实现集群从当前状态向期望状态迁移&lt;/span&gt;
&lt;/a&gt; 以组的形式创建和管理。&lt;/p&gt;
&lt;p&gt;下面的 &lt;a href=&#34;#pods-and-controllers&#34;&gt;Pods 与 controllers&lt;/a&gt; 有更多关于 k8s 是怎么通过工作负载资源和相应的控制器实现应用的扩容和自我恢复的&lt;/p&gt;
&lt;h3 id=&#34;pod-怎么管理多个容器&#34;&gt;Pod 怎么管理多个容器&lt;/h3&gt;
&lt;!--
Pods are designed to support multiple cooperating processes (as containers) that form
a cohesive unit of service. The containers in a Pod are automatically co-located and
co-scheduled on the same physical or virtual machine in the cluster. The containers
can share resources and dependencies, communicate with one another, and coordinate
when and how they are terminated.

For example, you might have a container that
acts as a web server for files in a shared volume, and a separate &#34;sidecar&#34; container
that updates those files from a remote source, as in the following diagram:

&lt;figure&gt;
    &lt;img src=&#34;https://lostsquirrel.github.io/k8sDocs/images/docs/pod.svg&#34;
         alt=&#34;example pod diagram&#34; width=&#34;50%&#34;/&gt; 
&lt;/figure&gt;


Some Pods have &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/.Site.baseURL/reference/glossary/?all=true#term-init-container&#39; target=&#39;_blank&#39;&gt;init containers&lt;span class=&#39;tooltip-text&#39;&gt;One or more initialization containers that must run to completion before any app containers run.&lt;/span&gt;
&lt;/a&gt; as well as &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/.Site.baseURL/reference/glossary/?all=true#term-app-container&#39; target=&#39;_blank&#39;&gt;app containers&lt;span class=&#39;tooltip-text&#39;&gt;A container used to run part of a workload. Compare with init container.&lt;/span&gt;
&lt;/a&gt;. Init containers run and complete before the app containers are started.

Pods natively provide two kinds of shared resources for their constituent containers:
[networking](#pod-networking) and [storage](#pod-storage).
--&gt;
&lt;p&gt;Pod 在设计上就支持多个协作进程(容器进程)组成一个服务。Pod 中的所有容器都会自动的同时调度到集群中的同一个物理机或虚拟机上。 这些容器可以共享资源和信赖，相互通信，并能协调什么时候以什么样的方式来终止容器。&lt;/p&gt;
&lt;p&gt;例如，用户可能有一个容器作为 web 服务，为共享数据卷的文件提供访问服务，另一个独立的容器作为 边车，负责从远程的源更新这些文件， 如下图所示:&lt;/p&gt;
&lt;figure&gt;
    &lt;img src=&#34;https://lostsquirrel.github.io/k8sDocs/k8sDocs/images/docs/pod.svg&#34;
         alt=&#34;example pod diagram&#34; width=&#34;50%&#34;/&gt; 
&lt;/figure&gt;

&lt;p&gt;有些 Pod 中会包含 &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/.Site.baseURL/reference/glossary/?all=true#term-init-container&#39; target=&#39;_blank&#39;&gt;init containers&lt;span class=&#39;tooltip-text&#39;&gt;One or more initialization containers that must run to completion before any app containers run.&lt;/span&gt;
&lt;/a&gt;， &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/.Site.baseURL/reference/glossary/?all=true#term-app-container&#39; target=&#39;_blank&#39;&gt;app containers&lt;span class=&#39;tooltip-text&#39;&gt;A container used to run part of a workload. Compare with init container.&lt;/span&gt;
&lt;/a&gt;。 初始化化容器在应用容器开启之前运行并完成。&lt;/p&gt;
&lt;p&gt;在同一个 Pod 中的容器天然共享两种资源: &lt;a href=&#34;#pod-networking&#34;&gt;网络&lt;/a&gt; 和 &lt;a href=&#34;#pod-storage&#34;&gt;存储&lt;/a&gt;.&lt;/p&gt;
&lt;h2 id=&#34;pod-操作&#34;&gt;Pod 操作&lt;/h2&gt;
&lt;!--
You&#39;ll rarely create individual Pods directly in Kubernetes—even singleton Pods. This
is because Pods are designed as relatively ephemeral, disposable entities. When
a Pod gets created (directly by you, or indirectly by a
&lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/2-concepts/01-architecture/02-controller/&#39; target=&#39;_blank&#39;&gt;controller&lt;span class=&#39;tooltip-text&#39;&gt;一个控制回路，负责通过 apiserver 监控集群的共享状态，并尝试通过变更(某些对象)的方式实现集群从当前状态向期望状态迁移&lt;/span&gt;
&lt;/a&gt;), the new Pod is
scheduled to run on a &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/2-concepts/01-architecture/00-nodes/&#39; target=&#39;_blank&#39;&gt;节点&lt;span class=&#39;tooltip-text&#39;&gt;一个节点就是 k8s 中的一个工作机&lt;/span&gt;
&lt;/a&gt; in your cluster.
The Pod remains on that node until the Pod finishes execution, the Pod object is deleted,
the Pod is *evicted* for lack of resources, or the node fails.

&lt;blockquote class=&#34;note&#34;&gt;
  &lt;div&gt;&lt;strong&gt;说明：&lt;/strong&gt; Restarting a container in a Pod should not be confused with restarting a Pod. A Pod
is not a process, but an environment for running container(s). A Pod persists until
it is deleted.&lt;/div&gt;
&lt;/blockquote&gt;


When you create the manifest for a Pod object, make sure the name specified is a valid
[DNS subdomain name](/docs/concepts/overview/working-with-objects/names#dns-subdomain-names).
--&gt;
&lt;p&gt;用户一般很少在 k8s 中直接创建独立的 Pod，即便是 singleton Pod。 这是因为在设计上，Pod 相对来是的临时的， 一次性的实体。
当一个 Pod 被创建之后(无论是用户直接创建，还是能过 &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/2-concepts/01-architecture/02-controller/&#39; target=&#39;_blank&#39;&gt;controller&lt;span class=&#39;tooltip-text&#39;&gt;一个控制回路，负责通过 apiserver 监控集群的共享状态，并尝试通过变更(某些对象)的方式实现集群从当前状态向期望状态迁移&lt;/span&gt;
&lt;/a&gt; 间接创建)，这个新建的 Pod 都会调度并运行在集群中的一个&lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/2-concepts/01-architecture/00-nodes/&#39; target=&#39;_blank&#39;&gt;节点&lt;span class=&#39;tooltip-text&#39;&gt;一个节点就是 k8s 中的一个工作机&lt;/span&gt;
&lt;/a&gt;上。
这个 Pod 会存在于这个节点上直接执行完成，然后这个 Pod 对象就会被删除掉，Pod 也可能因为资源不足或节点挂掉而被 &lt;em&gt;驱逐(evicted)&lt;/em&gt;&lt;/p&gt;
&lt;blockquote class=&#34;note&#34;&gt;
  &lt;div&gt;&lt;strong&gt;说明：&lt;/strong&gt; 重启 Pod 中的容器和重启一个 Pod 可能会搞混。 一个 Pod 并不是一个进程，而是运行容器的一个环境。一个 Pod 在被删除前都是一直存在&lt;/div&gt;
&lt;/blockquote&gt;

&lt;p&gt;在创建 Pod 对象的定义文件时， 名称必须是一个有效的 &lt;a href=&#34;../../../00-overview/03-working-with-objects/01-names/#dns-subdomain-names&#34;&gt;DNS 子域名&lt;/a&gt;&lt;/p&gt;
&lt;h3 id=&#34;hahahugoshortcode-s28-hbhb-和-hahahugoshortcode-s29-hbhb&#34;&gt;&lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/pods/&#39; target=&#39;_blank&#39;&gt;Pod&lt;span class=&#39;tooltip-text&#39;&gt;Pod 表示集群中运行的一组容器的集合&lt;/span&gt;
&lt;/a&gt; 和 &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/2-concepts/01-architecture/02-controller/&#39; target=&#39;_blank&#39;&gt;控制器&lt;span class=&#39;tooltip-text&#39;&gt;一个控制回路，负责通过 apiserver 监控集群的共享状态，并尝试通过变更(某些对象)的方式实现集群从当前状态向期望状态迁移&lt;/span&gt;
&lt;/a&gt;&lt;/h3&gt;
&lt;!--
You can use workload resources to create and manage multiple Pods for you. A controller
for the resource handles replication and rollout and automatic healing in case of
Pod failure. For example, if a Node fails, a controller notices that Pods on that
Node have stopped working and creates a replacement Pod. The scheduler places the
replacement Pod onto a healthy Node.

Here are some examples of workload resources that manage one or more Pods:

* &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/deployment/&#39; target=&#39;_blank&#39;&gt;Deployment&lt;span class=&#39;tooltip-text&#39;&gt;管理集群中的一个多副本应用&lt;/span&gt;
&lt;/a&gt;
* &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/statefulset/&#39; target=&#39;_blank&#39;&gt;StatefulSet&lt;span class=&#39;tooltip-text&#39;&gt;管理一个 Pod 集合的部署与容量伸缩， 这些 Pod 所以使用的存储是持久的(Pod 被替代后，新的 Pod 继承老 Pod 的存储)， Pod 的标识也是持久化的(重建 Pod 后名字不会变)&lt;/span&gt;
&lt;/a&gt;
* &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/daemonset&#39; target=&#39;_blank&#39;&gt;DaemonSet&lt;span class=&#39;tooltip-text&#39;&gt;确保 Pod 运行在集群中的节点上&lt;/span&gt;
&lt;/a&gt;
--&gt;
&lt;p&gt;用户可以使用工作负载资源来创建和管理多个 Pod。 资源的控制器处理 副本， 回滚，在 Pod 持掉时自动恢复。
例如， 如果一个节点挂了，一个控制器注意到这个节点上的 Pod 都不工作了，则会创建一个替代的 Pod。
然后调度器将这些替代 Pod 放到健康的节点上。
以下为常见的可以管理一个或多个 Pod 的工作负载示例:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/deployment/&#39; target=&#39;_blank&#39;&gt;Deployment&lt;span class=&#39;tooltip-text&#39;&gt;管理集群中的一个多副本应用&lt;/span&gt;
&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/statefulset/&#39; target=&#39;_blank&#39;&gt;StatefulSet&lt;span class=&#39;tooltip-text&#39;&gt;管理一个 Pod 集合的部署与容量伸缩， 这些 Pod 所以使用的存储是持久的(Pod 被替代后，新的 Pod 继承老 Pod 的存储)， Pod 的标识也是持久化的(重建 Pod 后名字不会变)&lt;/span&gt;
&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/daemonset&#39; target=&#39;_blank&#39;&gt;DaemonSet&lt;span class=&#39;tooltip-text&#39;&gt;确保 Pod 运行在集群中的节点上&lt;/span&gt;
&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;pod-模板&#34;&gt;Pod 模板&lt;/h3&gt;
&lt;!--
Controllers for &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/2-concepts/03-workloads/&#39; target=&#39;_blank&#39;&gt;workload&lt;span class=&#39;tooltip-text&#39;&gt;一个工作负载就是一个运行在k8s上的应用&lt;/span&gt;
&lt;/a&gt; resources create Pods
from a _pod template_ and manage those Pods on your behalf.

PodTemplates are specifications for creating Pods, and are included in workload resources such as
[Deployments](/docs/concepts/workloads/controllers/deployment/),
[Jobs](/docs/concepts/jobs/run-to-completion-finite-workloads/), and
[DaemonSets](/docs/concepts/workloads/controllers/daemonset/).

Each controller for a workload resource uses the `PodTemplate` inside the workload
object to make actual Pods. The `PodTemplate` is part of the desired state of whatever
workload resource you used to run your app.
 --&gt;
&lt;p&gt;&lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/2-concepts/03-workloads/&#39; target=&#39;_blank&#39;&gt;工作负载&lt;span class=&#39;tooltip-text&#39;&gt;一个工作负载就是一个运行在k8s上的应用&lt;/span&gt;
&lt;/a&gt; 资源对应的控制器通过用户提供的 &lt;em&gt;Pod 模板&lt;/em&gt; 来创建和管理这个 Pod。
诸如
&lt;a href=&#34;https://lostsquirrel.github.io/k8sDocs/docs/concepts/workloads/controllers/deployment/&#34;&gt;Deployments&lt;/a&gt;,
&lt;a href=&#34;https://lostsquirrel.github.io/k8sDocs/docs/concepts/jobs/run-to-completion-finite-workloads/&#34;&gt;Jobs&lt;/a&gt;,
&lt;a href=&#34;https://lostsquirrel.github.io/k8sDocs/docs/concepts/workloads/controllers/daemonset/&#34;&gt;DaemonSets&lt;/a&gt;的&lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/2-concepts/03-workloads/&#39; target=&#39;_blank&#39;&gt;工作负载&lt;span class=&#39;tooltip-text&#39;&gt;一个工作负载就是一个运行在k8s上的应用&lt;/span&gt;
&lt;/a&gt; 资源都会包含创建 Pod 的定义字段 &lt;code&gt;PodTemplates&lt;/code&gt;。
一个&lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/2-concepts/03-workloads/&#39; target=&#39;_blank&#39;&gt;工作负载&lt;span class=&#39;tooltip-text&#39;&gt;一个工作负载就是一个运行在k8s上的应用&lt;/span&gt;
&lt;/a&gt; 资源对应的控制通过工作负载对象中的 &lt;code&gt;PodTemplate&lt;/code&gt; 来创建实际的 Pod。 &lt;code&gt;PodTemplate&lt;/code&gt; 是用户用于运行应用的工作负载的期望状态的一部分。&lt;/p&gt;
&lt;!--
The sample below is a manifest for a simple Job with a `template` that starts one
container. The container in that Pod prints a message then pauses.

```yaml
apiVersion: batch/v1
kind: Job
metadata:
  name: hello
spec:
  template:
    # This is the pod template
    spec:
      containers:
      - name: hello
        image: busybox
        command: [&#39;sh&#39;, &#39;-c&#39;, &#39;echo &#34;Hello, Kubernetes!&#34; &amp;&amp; sleep 3600&#39;]
      restartPolicy: OnFailure
    # The pod template ends here
```
 --&gt;
&lt;p&gt;以下为一个简单 &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/job/&#39; target=&#39;_blank&#39;&gt;Job&lt;span class=&#39;tooltip-text&#39;&gt;一个运行后即结束的有限或批量任务&lt;/span&gt;
&lt;/a&gt; 的定义文件，其中 &lt;code&gt;template&lt;/code&gt; 定义会启动一个容器。
Pod 中的这个容器会打包一条信息，然后睡一个小时(3600秒)&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-yaml&#34; data-lang=&#34;yaml&#34;&gt;&lt;span style=&#34;color:#f92672&#34;&gt;apiVersion&lt;/span&gt;: &lt;span style=&#34;color:#ae81ff&#34;&gt;batch/v1&lt;/span&gt;
&lt;span style=&#34;color:#f92672&#34;&gt;kind&lt;/span&gt;: &lt;span style=&#34;color:#ae81ff&#34;&gt;Job&lt;/span&gt;
&lt;span style=&#34;color:#f92672&#34;&gt;metadata&lt;/span&gt;:
  &lt;span style=&#34;color:#f92672&#34;&gt;name&lt;/span&gt;: &lt;span style=&#34;color:#ae81ff&#34;&gt;hello&lt;/span&gt;
&lt;span style=&#34;color:#f92672&#34;&gt;spec&lt;/span&gt;:
  &lt;span style=&#34;color:#f92672&#34;&gt;template&lt;/span&gt;:
    &lt;span style=&#34;color:#75715e&#34;&gt;# Pod 模板从这里开始&lt;/span&gt;
    &lt;span style=&#34;color:#f92672&#34;&gt;spec&lt;/span&gt;:
      &lt;span style=&#34;color:#f92672&#34;&gt;containers&lt;/span&gt;:
      - &lt;span style=&#34;color:#f92672&#34;&gt;name&lt;/span&gt;: &lt;span style=&#34;color:#ae81ff&#34;&gt;hello&lt;/span&gt;
        &lt;span style=&#34;color:#f92672&#34;&gt;image&lt;/span&gt;: &lt;span style=&#34;color:#ae81ff&#34;&gt;busybox&lt;/span&gt;
        &lt;span style=&#34;color:#f92672&#34;&gt;command&lt;/span&gt;: [&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#39;sh&amp;#39;&lt;/span&gt;, &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#39;-c&amp;#39;&lt;/span&gt;, &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#39;echo &amp;#34;Hello, Kubernetes!&amp;#34; &amp;amp;&amp;amp; sleep 3600&amp;#39;&lt;/span&gt;]
      &lt;span style=&#34;color:#f92672&#34;&gt;restartPolicy&lt;/span&gt;: &lt;span style=&#34;color:#ae81ff&#34;&gt;OnFailure&lt;/span&gt;
    &lt;span style=&#34;color:#75715e&#34;&gt;# Pod 模板到这里结束&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;!--
Modifying the pod template or switching to a new pod template has no effect on the
Pods that already exist. Pods do not receive template updates directly. Instead,
a new Pod is created to match the revised pod template.

For example, the deployment controller ensures that the running Pods match the current
pod template for each Deployment object. If the template is updated, the Deployment has
to remove the existing Pods and create new Pods based on the updated template. Each workload
resource implements its own rules for handling changes to the Pod template.

On Nodes, the &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/docs/reference/generated/kubelet&#39; target=&#39;_blank&#39;&gt;kubelet&lt;span class=&#39;tooltip-text&#39;&gt;An agent that runs on each node in the cluster. It makes sure that containers are running in a pod.&lt;/span&gt;
&lt;/a&gt; does not
directly observe or manage any of the details around pod templates and updates; those
details are abstracted away. That abstraction and separation of concerns simplifies
system semantics, and makes it feasible to extend the cluster&#39;s behavior without
changing existing code.

 --&gt;
&lt;p&gt;修改 Pod 模板甚至替换为新的模板都不会对已经存在的 Pod 有任何影响。 Pod 不接收直接的模板更新，而是基于新的模板创建一个新的 Pod。
例如， &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/deployment/&#39; target=&#39;_blank&#39;&gt;Deployment&lt;span class=&#39;tooltip-text&#39;&gt;管理集群中的一个多副本应用&lt;/span&gt;
&lt;/a&gt; 控制器会保证每个 &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/deployment/&#39; target=&#39;_blank&#39;&gt;Deployment&lt;span class=&#39;tooltip-text&#39;&gt;管理集群中的一个多副本应用&lt;/span&gt;
&lt;/a&gt; 对象对应的 Pod 与其械反定义是一致的.
如果 &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/deployment/&#39; target=&#39;_blank&#39;&gt;Deployment&lt;span class=&#39;tooltip-text&#39;&gt;管理集群中的一个多副本应用&lt;/span&gt;
&lt;/a&gt; Pod 模板有更新，就对移除旧模板对应的 Pod，并以新模板创建新的 Pod。 每个工作负载资源都实现了一个自己的模板更新处理方式。&lt;/p&gt;
&lt;p&gt;在节点上， &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/docs/reference/generated/kubelet&#39; target=&#39;_blank&#39;&gt;kubelet&lt;span class=&#39;tooltip-text&#39;&gt;An agent that runs on each node in the cluster. It makes sure that containers are running in a pod.&lt;/span&gt;
&lt;/a&gt; 并不会直接监测或管理 Pod 模板的详情或更新；这些详情都已经被抽象剥离。
通过抽象和分离系统关注点，使得可以在不动已存在代码的情况下轻松实现对集群行为的扩展。
&lt;blockquote&gt;
  &lt;div&gt;&lt;strong&gt;TODO: &lt;/strong&gt;说得不太清楚，需要实践理解后再考虑怎么修改&lt;/div&gt;
&lt;/blockquote&gt;
&lt;/p&gt;
&lt;h2 id=&#34;资源共享和网络通信&#34;&gt;资源共享和网络通信&lt;/h2&gt;
&lt;!--
## Resources sharing and communications
Pods enable data sharing and communication among their constituent
containters.
--&gt;
&lt;p&gt;Pod 为内部容器提供了数据和网络的共享环境&lt;/p&gt;
&lt;!--
### Storage in Pods {#pod-storage}

A Pod can specify a set of shared storage
&lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/docs/concepts/storage/volumes/&#39; target=&#39;_blank&#39;&gt;volumes&lt;span class=&#39;tooltip-text&#39;&gt;A directory containing data, accessible to the containers in a pod.&lt;/span&gt;
&lt;/a&gt;. All containers
in the Pod can access the shared volumes, allowing those containers to
share data. Volumes also allow persistent data in a Pod to survive
in case one of the containers within needs to be restarted. See
[Storage](/docs/concepts/storage/) for more information on how
Kubernetes implements shared storage and makes it available to Pods.

 --&gt;
&lt;h3 id=&#34;pod-storage&#34;&gt;Pod 的存储&lt;/h3&gt;
&lt;p&gt;一个 Pod 可以指定一组共享的存储 &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/docs/concepts/storage/volumes/&#39; target=&#39;_blank&#39;&gt;Volume&lt;span class=&#39;tooltip-text&#39;&gt;A directory containing data, accessible to the containers in a pod.&lt;/span&gt;
&lt;/a&gt;。
这个 Pod 内的所有容器都可以使用这个共享卷， 实现容器间的数据共享。卷也可以让其中的数据在， Pod
被销毁重建后依然存在。更多关于 k8s 怎么实现共享存储及其在 Pod 中的使用，见 &lt;a href=&#34;../../05-storage/&#34;&gt;存储&lt;/a&gt;&lt;/p&gt;
&lt;!--
### Pod networking

Each Pod is assigned a unique IP address for each address family. Every
container in a Pod shares the network namespace, including the IP address and
network ports. Inside a Pod (and **only** then), the containers that belong to the Pod
can communicate with one another using `localhost`. When containers in a Pod communicate
with entities *outside the Pod*,
they must coordinate how they use the shared network resources (such as ports).
Within a Pod, containers share an IP address and port space, and
can find each other via `localhost`. The containers in a Pod can also communicate
with each other using standard inter-process communications like SystemV semaphores
or POSIX shared memory.  Containers in different Pods have distinct IP addresses
and can not communicate by IPC without
[special configuration](/docs/concepts/policy/pod-security-policy/).
Containers that want to interact with a container running in a different Pod can
use IP networking to comunicate.

Containers within the Pod see the system hostname as being the same as the configured
`name` for the Pod. There&#39;s more about this in the [networking](/docs/concepts/cluster-administration/networking/)
section.


 --&gt;
&lt;h3 id=&#34;pod-的网络&#34;&gt;Pod 的网络&lt;/h3&gt;
&lt;p&gt;每个 Pod 在一个地址族内分配唯一IP地址，在Pod内的所有容器共享网络空间，包括IP地址和网络端口，
在一个Pod中(且仅在此时)， 属于该 Pod的容器相互之间可以通过 &lt;code&gt;localhost&lt;/code&gt;通信。 当 Pod 中的容器
与 &lt;em&gt;Pod 外&lt;/em&gt; 的实体通信时需要协调好，它们(Pod 中的容器)之间怎么使用共享的网络资源(如端口)。
在同一个Pod中的容器共享IP地址和端口池，并且相互之间可以通过 &lt;code&gt;localhost&lt;/code&gt; 找到彼此。
在同一个Pod中的容器还可以使用如 SystemV 信号量 或 POSIX 共享内存之类的标准进程间通信。
不同Pod中的容器拥有不同的IP地址，并且在不进行&lt;a href=&#34;../../08-policy/02-pod-security-policy/&#34;&gt;特殊配置&lt;/a&gt;的情况下不能通过进程间通信(IPC)&lt;/p&gt;
&lt;p&gt;Pod 中的容器看到的系统主机名就是定义文件中这个 Pod 的 &lt;code&gt;name&lt;/code&gt; 字段的值。 更多相关信息请见 &lt;a href=&#34;../../10-cluster-administration/03-networking/&#34;&gt;网络&lt;/a&gt;&lt;/p&gt;
&lt;!--
## Privileged mode for containers

Any container in a Pod can enable privileged mode, using the `privileged` flag on the [security context](/docs/tasks/configure-pod-container/security-context/) of the container spec. This is useful for containers that want to use operating system administrative capabilities such as manipulating the network stack or accessing hardware devices.
Processes within a privileged container get almost the same privileges that are available to processes outside a container.

&lt;blockquote class=&#34;note&#34;&gt;
  &lt;div&gt;&lt;strong&gt;说明：&lt;/strong&gt; Your &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/docs/setup/production-environment/container-runtimes&#39; target=&#39;_blank&#39;&gt;container runtime&lt;span class=&#39;tooltip-text&#39;&gt;容器运行环境就是负责运行容器的软件&lt;/span&gt;
&lt;/a&gt; must support the concept of a privileged container for this setting to be relevant.&lt;/div&gt;
&lt;/blockquote&gt;


 --&gt;
&lt;h2 id=&#34;容器的提权模式&#34;&gt;容器的提权模式&lt;/h2&gt;
&lt;p&gt;Pod 中的任意容器都可以开启提权模式，通过容器定义中的 &lt;a href=&#34;../../../3-tasks/02-configure-pod-container/09-security-context/&#34;&gt;安全上下文&lt;/a&gt; 中有一个 &lt;code&gt;privileged&lt;/code&gt; 标志 开启或关闭(默认是关闭的). 这个模式对需要使用操作系统管理员权限如操作网络栈或访问硬件设备相当有用。
在提权模式开启的容器中的进行基本与容器外的的进程拥有相同的权限。&lt;/p&gt;
&lt;blockquote class=&#34;note&#34;&gt;
  &lt;div&gt;&lt;strong&gt;说明：&lt;/strong&gt; 提权模式生效需要 &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/docs/setup/production-environment/container-runtimes&#39; target=&#39;_blank&#39;&gt;容器运行环境&lt;span class=&#39;tooltip-text&#39;&gt;容器运行环境就是负责运行容器的软件&lt;/span&gt;
&lt;/a&gt; 支持提权容器这个概念。&lt;/div&gt;
&lt;/blockquote&gt;

&lt;!--
## Static Pods

_Static Pods_ are managed directly by the kubelet daemon on a specific node,
without the &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/docs/reference/generated/kube-apiserver/&#39; target=&#39;_blank&#39;&gt;API server&lt;span class=&#39;tooltip-text&#39;&gt;Control plane component that serves the Kubernetes API.&lt;/span&gt;
&lt;/a&gt;
observing them.
Whereas most Pods are managed by the control plane (for example, a
&lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/deployment/&#39; target=&#39;_blank&#39;&gt;Deployment&lt;span class=&#39;tooltip-text&#39;&gt;管理集群中的一个多副本应用&lt;/span&gt;
&lt;/a&gt;), for static
Pods, the kubelet directly supervises each static Pod (and restarts it if it fails).

Static Pods are always bound to one &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/docs/reference/generated/kubelet&#39; target=&#39;_blank&#39;&gt;Kubelet&lt;span class=&#39;tooltip-text&#39;&gt;An agent that runs on each node in the cluster. It makes sure that containers are running in a pod.&lt;/span&gt;
&lt;/a&gt; on a specific node.
The main use for static Pods is to run a self-hosted control plane: in other words,
using the kubelet to supervise the individual [control plane components](/docs/concepts/overview/components/#control-plane-components).

The kubelet automatically tries to create a &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/.Site.baseURL/reference/glossary/?all=true#term-mirror-pod&#39; target=&#39;_blank&#39;&gt;mirror Pod&lt;span class=&#39;tooltip-text&#39;&gt;用于追踪 kubelet 监管的静态 Pod，存在于 api-server 中的对象&lt;/span&gt;
&lt;/a&gt;
on the Kubernetes API server for each static Pod.
This means that the Pods running on a node are visible on the API server,
but cannot be controlled from there.

 --&gt;
&lt;h2 id=&#34;静态-pod&#34;&gt;静态 Pod&lt;/h2&gt;
&lt;p&gt;&lt;em&gt;静态 Pod&lt;/em&gt; 是由节点上的 kubelet 直接管理的 Pod， 并不被 &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/docs/reference/generated/kube-apiserver/&#39; target=&#39;_blank&#39;&gt;API server&lt;span class=&#39;tooltip-text&#39;&gt;Control plane component that serves the Kubernetes API.&lt;/span&gt;
&lt;/a&gt;
监控。 然而多数 Pod 都是受控制中心管理(例，&lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/deployment/&#39; target=&#39;_blank&#39;&gt;Deployment&lt;span class=&#39;tooltip-text&#39;&gt;管理集群中的一个多副本应用&lt;/span&gt;
&lt;/a&gt;)，
静态 Pod 则是直接由 kubelet 监控和管理(如果挂也是由 kubelet 来重启)。
静态 Pod 总是与节点上的 &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/docs/reference/generated/kubelet&#39; target=&#39;_blank&#39;&gt;Kubelet&lt;span class=&#39;tooltip-text&#39;&gt;An agent that runs on each node in the cluster. It makes sure that containers are running in a pod.&lt;/span&gt;
&lt;/a&gt; 绑定在一起的。
静态 Pod 主要用于运行自建(相对于云提供商提供)控制中心，也就是用 kubelet 来监管各个&lt;a href=&#34;../../00-overview/01-components/#control-plane-components&#34;&gt;控制中心组件&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;kubelet 会自动的尝试在 &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/docs/reference/generated/kube-apiserver/&#39; target=&#39;_blank&#39;&gt;API server&lt;span class=&#39;tooltip-text&#39;&gt;Control plane component that serves the Kubernetes API.&lt;/span&gt;
&lt;/a&gt; 上为每个静态 Pod 去创建 &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/.Site.baseURL/reference/glossary/?all=true#term-mirror-pod&#39; target=&#39;_blank&#39;&gt;镜像 Pod&lt;span class=&#39;tooltip-text&#39;&gt;用于追踪 kubelet 监管的静态 Pod，存在于 api-server 中的对象&lt;/span&gt;
&lt;/a&gt;。
也就是说这些运行在节点上的Pod在 &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/docs/reference/generated/kube-apiserver/&#39; target=&#39;_blank&#39;&gt;API server&lt;span class=&#39;tooltip-text&#39;&gt;Control plane component that serves the Kubernetes API.&lt;/span&gt;
&lt;/a&gt; 上也是可以看到的，但不能通过 &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/docs/reference/generated/kube-apiserver/&#39; target=&#39;_blank&#39;&gt;API server&lt;span class=&#39;tooltip-text&#39;&gt;Control plane component that serves the Kubernetes API.&lt;/span&gt;
&lt;/a&gt; 对其进行修改。&lt;/p&gt;
&lt;h2 id=&#34;相关资料&#34;&gt;相关资料&lt;/h2&gt;
&lt;!--

* Learn about the [lifecycle of a Pod](/docs/concepts/workloads/pods/pod-lifecycle/).
* Learn about [PodPresets](/docs/concepts/workloads/pods/podpreset/).
* Lean about [RuntimeClass](/docs/concepts/containers/runtime-class/) and how you can use it to
  configure different Pods with different container runtime configurations.
* Read about [Pod topology spread constraints](/docs/concepts/workloads/pods/pod-topology-spread-constraints/).
* Read about [PodDisruptionBudget](https://kubernetes.io/docs/concepts/workloads/pods/disruptions/) and how you can use it to manage application availability during disruptions.
* Pod is a top-level resource in the Kubernetes REST API.  
  The [Pod](/docs/reference/generated/kubernetes-api/v1.19/#pod-v1-core)
  object definition describes the object in detail.
* [The Distributed System Toolkit: Patterns for Composite Containers](https://kubernetes.io/blog/2015/06/the-distributed-system-toolkit-patterns) explains common layouts for Pods with more than one container.

To understand the context for why Kubernetes wraps a common Pod API in other resources (such as &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/statefulset/&#39; target=&#39;_blank&#39;&gt;StatefulSets&lt;span class=&#39;tooltip-text&#39;&gt;管理一个 Pod 集合的部署与容量伸缩， 这些 Pod 所以使用的存储是持久的(Pod 被替代后，新的 Pod 继承老 Pod 的存储)， Pod 的标识也是持久化的(重建 Pod 后名字不会变)&lt;/span&gt;
&lt;/a&gt; or &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/deployment/&#39; target=&#39;_blank&#39;&gt;Deployments&lt;span class=&#39;tooltip-text&#39;&gt;管理集群中的一个多副本应用&lt;/span&gt;
&lt;/a&gt;, you can read about the prior art, including:
  * [Aurora](http://aurora.apache.org/documentation/latest/reference/configuration/#job-schema)
  * [Borg](https://research.google.com/pubs/pub43438.html)
  * [Marathon](https://mesosphere.github.io/marathon/docs/rest-api.html)
  * [Omega](https://research.google/pubs/pub41684/)
  * [Tupperware](https://engineering.fb.com/data-center-engineering/tupperware/).
 --&gt;
&lt;ul&gt;
&lt;li&gt;了解 &lt;a href=&#34;../00-pod-lifecycle/&#34;&gt;Pod生命周期&lt;/a&gt;.&lt;/li&gt;
&lt;li&gt;了解 &lt;a href=&#34;../03-podpreset/&#34;&gt;PodPresets&lt;/a&gt;.&lt;/li&gt;
&lt;li&gt;了解 &lt;a href=&#34;../../02-containers/02runtime-class/&#34;&gt;RuntimeClass&lt;/a&gt; 并如何使用它来在不同的运行时上运行不同的 Pod&lt;/li&gt;
&lt;li&gt;了解 &lt;a href=&#34;../02-pod-topology-spread-constraints/&#34;&gt;Pod topology spread constraints&lt;/a&gt;.&lt;/li&gt;
&lt;li&gt;了解 &lt;a href=&#34;https://kubernetes.io/docs/concepts/workloads/pods/disruptions/&#34;&gt;PodDisruptionBudget&lt;/a&gt; 并用它来在干扰时管理应用的可用性&lt;/li&gt;
&lt;li&gt;Pod k8s REST API 中顶级资源.&lt;br&gt;
这里是 &lt;a href=&#34;https://lostsquirrel.github.io/k8sDocs/docs/reference/generated/kubernetes-api/v1.19/#pod-v1-core&#34;&gt;Pod&lt;/a&gt;
对象文档&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://kubernetes.io/blog/2015/06/the-distributed-system-toolkit-patterns&#34;&gt;The Distributed System Toolkit: Patterns for Composite Containers&lt;/a&gt; 介绍多容器 Pod 的常用布局&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;要理解为啥k8s 要把 Pod 包在其它(如 &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/statefulset/&#39; target=&#39;_blank&#39;&gt;StatefulSets&lt;span class=&#39;tooltip-text&#39;&gt;管理一个 Pod 集合的部署与容量伸缩， 这些 Pod 所以使用的存储是持久的(Pod 被替代后，新的 Pod 继承老 Pod 的存储)， Pod 的标识也是持久化的(重建 Pod 后名字不会变)&lt;/span&gt;
&lt;/a&gt; 或 &lt;a class=&#39;glossary-tooltip&#39; href=&#39;https://lostsquirrel.github.io/k8sDocs/k8sDocs/concepts/workloads/controllers/deployment/&#39; target=&#39;_blank&#39;&gt;Deployments&lt;span class=&#39;tooltip-text&#39;&gt;管理集群中的一个多副本应用&lt;/span&gt;
&lt;/a&gt;)的资源中，请阅读以下先前技术(prior art)：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;http://aurora.apache.org/documentation/latest/reference/configuration/#job-schema&#34;&gt;Aurora&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://research.google.com/pubs/pub43438.html&#34;&gt;Borg&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://mesosphere.github.io/marathon/docs/rest-api.html&#34;&gt;Marathon&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://research.google/pubs/pub41684/&#34;&gt;Omega&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://engineering.fb.com/data-center-engineering/tupperware/&#34;&gt;Tupperware&lt;/a&gt;.&lt;/li&gt;
&lt;/ul&gt;

      </description>
    </item>
    
    <item>
      <title>Docs: 控制器</title>
      <link>https://lostsquirrel.github.io/k8sDocs/docs/concepts/workloads/controllers/</link>
      <pubDate>Thu, 02 Jul 2020 00:00:00 +0000</pubDate>
      
      <guid>https://lostsquirrel.github.io/k8sDocs/docs/concepts/workloads/controllers/</guid>
      <description>
        
        
        
      </description>
    </item>
    
  </channel>
</rss>
